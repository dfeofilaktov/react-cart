// import PropTypes from 'prop-types';
// import classnames from 'classnames';

const Header = () => {
    return (
        <header>
            <div className='hello'>React Cart</div>
        </header>
    );
};

Header.propTypes = {};

Header.defaultProps = {};

export default Header;
